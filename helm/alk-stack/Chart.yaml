apiVersion: v2
name: alk-stack
description: Angular + ASP.NET + MongoDB stack for alk-stack
type: application
version: 0.1.0
appVersion: "1.0.0"
dependencies:
  - name: mongodb
    version: ">=16.0.0 <17.0.0"
    repository: "https://charts.bitnami.com/bitnami"
    condition: mongodb.enabled
